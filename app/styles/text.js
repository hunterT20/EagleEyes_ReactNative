import {StyleSheet} from "react-native"
const text_style = StyleSheet.create({
  welcome: {
    color: 'white',
    fontSize: 24,
    textAlign: 'center',
    margin: 10,
  }
});

export default text_style;
